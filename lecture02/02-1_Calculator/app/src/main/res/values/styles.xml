<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="fun_buttons_style">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/fun_buttons_shape</item>
    </style>

    <style name="sign_buttons_style">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/sign_buttons_shape</item>
    </style>

    <style name="num_buttons_style">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/num_buttons_shape</item>
    </style>

    <style name="zero_button_style">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/zero_button_shape</item>
    </style>

</resources>